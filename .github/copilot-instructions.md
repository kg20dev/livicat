

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v6.10.1 -->
<!-- Updated: 2026-06-12T16:10:23.541Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

# Code signatures

## deps
```
src/App.tsx ← components/ui/ErrorBoundary, components/layout/Sidebar, components/layout/TopBar, components/layout/PreviewArea, components/layout/StylingPanel
src/components/analytics/AnalyticsConsent.tsx ← ../utils/analytics
src/components/chat/ChatPreview.tsx ← ChatMessage
src/components/chat/__tests__/ChatIframe.test.tsx ← ChatIframe
src/components/layout/PreviewArea.tsx ← chat/ChatPreview, ui/UrlInputBar, ../utils/youtubeValidation, ../hooks/useElectronPreview, ../utils/youtubeMetadata
src/components/layout/Settings.tsx ← ../utils/analytics
src/components/layout/Sidebar.tsx ← ../../package, ../services
src/components/layout/StylingPanel.tsx ← ../hooks/useChatSettings, ../types/app, ../utils/fonts, ../utils/analytics
src/components/ui/__tests__/ErrorBoundary.test.tsx ← ErrorBoundary
src/hooks/useApiKey.ts ← utils/storage
src/hooks/useChatSettings.ts ← types, utils/cssGenerator, utils/fonts
src/hooks/useYouTubeChat.ts ← services/YouTubeService, services/ChatPollingService, types/youtube
src/services/ChatPollingService.ts ← YouTubeService, types/youtube
src/services/YouTubeService.ts ← types/youtube
src/utils/cssExport.ts ← analytics
src/utils/youtubeMetadata.ts ← analytics
```

## todos
```
src/utils/formatters.ts:24  # TODO: Implement proper sanitization for XSS prevention
```

## changes (last 10 commits — 2 hours ago)
```
src/components/layout/StylingPanel.tsx        +StylingPanelNameMessageLayout  +StylingPanelBackgroundStyle
src/hooks/useChatSettings.ts                  ~settingsToCSSSettings
src/utils/cssGenerator.ts                     +buildLayoutRules  ~buildAnimationRules  ~generateChatCSS
```

## .github

### .github/workflows/build-tauri.yml
```
keys: [name, on, env, permissions, jobs]
job: setup
job: build-app
job: build-mac
job: build-windows
job: release
```

### .github/workflows/build-windows-dev.yml
```
keys: [name, on, jobs]
job: build-windows-dev
```

### .github/workflows/ci.yml
```
keys: [name, on, jobs]
job: lint
job: type-check
job: test
job: rust-check
job: build
```

### .github/ISSUE_TEMPLATE/bug_report.md
```
h2 Bug Description
h2 Steps to Reproduce
h2 Expected Behavior
h2 Actual Behavior
h2 Screenshots
h2 Environment
h2 Additional Context
h2 Severity
```

### .github/ISSUE_TEMPLATE/bug_report.yml
```
keys: [name, description, labels, body]
```

### .github/ISSUE_TEMPLATE/feature_request.md
```
h2 Feature Description
h2 Problem Statement
h2 Proposed Solution
h2 Alternatives Considered
h2 Additional Context
h2 Priority
```

### .github/PULL_REQUEST_TEMPLATE.md
```
h2 Description
h2 Type of Change
h2 Related Issue
h2 Changes Made
h2 Testing
h2 Screenshots (if applicable)
h2 Checklist
h2 Additional Notes
```

### .github/workflows/auto-tag-and-release.yml
```
keys: [name, on, permissions, jobs]
job: auto-tag-and-release
```

## src

### src/App.css
```
.App
.card
```

### src/App.tsx
```
component App
hook useState
hook useRef
hook useEffect
hook useCallback
hook useMemo
handler onStartRef
handler onDecision
handler onNavigate
handler onExport
handler onTabChange
handler onUrlChange
handler onFetch
handler onCSSChange
```

### src/components/analytics/AnalyticsConsent.css
```
.consent-backdrop
.consent-backdrop
.consent-modal
.consent-modal
.consent-close
.consent-close
.consent-content
.consent-title
```

### src/components/analytics/AnalyticsConsent.tsx
```
component AnalyticsConsent
props AnalyticsConsentProps
hook useState
handler onClick
```

### src/components/chat/ChatIframe.tsx
```
component ChatIframe
props ChatIframeProps
hook useRef
hook useState
hook useEffect
hook useCallback
export ChatIframeError
handler onClick
```

### src/components/chat/ChatMessage.tsx
```
component ChatMessage
props ChatMessageProps
```

### src/components/chat/ChatPreview.tsx
```
component ChatPreview
component ChatPreviewHeader
component ChatPreviewMessages
props ChatPreviewRootProps
hook useChatPreviewContext
hook useContext
export Message
```

### src/components/chat/__tests__/ChatIframe.test.tsx
```
handler onLoad
handler onError
```

### src/components/layout/AssetsPage.tsx
```
component AssetsPage
component AssetsPageHeader
component AssetsPageToolbar
component AssetsPageGrid
component AssetsPageAssetCard
props AssetsPageRootProps
hook useAssetsPageContext
hook useContext
export AssetItem
handler onChange
handler onClick
```

### src/components/layout/PreviewArea.tsx
```
component PreviewArea
component PreviewAreaToolBar
component PreviewAreaVideoInfo
component PreviewAreaChat
component PreviewAreaActions
props PreviewAreaRootProps
hook usePreviewAreaContext
hook useContext
hook useMemo
hook useCallback
hook useElectronPreview
hook useState
hook useRef
hook useEffect
handler onRetryFetch
handler onTabChange
handler onUrlChange
handler onFetch
handler onClick
```

### src/components/layout/Settings.css
```
.settings-container
.settings-header
.settings-title
.settings-content
.settings-section
.settings-section-header
.settings-section-icon
.settings-section-icon
```

### src/components/layout/Settings.tsx
```
component Settings
hook useState
hook useEffect
handler onChange
```

### src/components/layout/Sidebar.tsx
```
component Sidebar
component SidebarHeader
component SidebarNav
component SidebarNavItems
component SidebarExportButton
props SidebarRootProps
hook useSidebarContext
hook useContext
hook useState
hook useEffect
handler onNavigate
handler onClick
```

### src/components/layout/StylingPanel.tsx
```
component StylingPanel
component StylingPanelHeader
component StylingPanelSection
component StylingPanelField
component StylingPanelControlGroup
component StylingPanelSlider
component StylingPanelToggle
component StylingPanelColorField
component StylingPanelSelect
component StylingPanelAnimationStyleSelector
component StylingPanelNumberField
component StylingPanelPresetSelector
component StylingPanelHeroSection
component StylingPanelNameMessageLayout
component StylingPanelBackgroundStyle
props StylingPanelRootProps
hook useStylingPanelContext
hook useContext
hook useChatSettings
hook useCallback
hook useMemo
hook useEffect
handler onChange
handler onStyleSelector
```

### src/components/layout/TopBar.tsx
```
component TopBar
component TopBarLeft
component TopBarRight
props TopBarRootProps
```

### src/components/loading/LoadingScreen.css
```
.loading-screen
.loading-screen
.loading-content
.icon-wrapper
.livicat-icon
.livicat-title
```

### src/components/loading/LoadingScreen.tsx
```
component LoadingScreen
props LoadingScreenProps
hook useState
hook useRef
hook useEffect
handler onCompleteRef
```

### src/components/ui/ErrorBoundary.tsx
```
props ErrorBoundaryProps
handler onClick
```

### src/components/ui/UrlInputBar.tsx
```
component UrlInputBar
component UrlInputBarTabs
component UrlInputBarInputSection
props UrlInputBarRootProps
hook useUrlInputBarContext
hook useContext
export ChatMode
handler onChange
handler onClick
```

### src/components/ui/__tests__/ErrorBoundary.test.tsx
```
component BuggyComponent
handler onError
```

### src/hooks/useApiKey.ts
```
export function useApiKey() → { apiKey, setApiKey, clearSavedKey, keySaved }
```

### src/hooks/useChatSettings.ts
```
export function settingsToCSSSettings(settings) → ChatCSSSettings
export function settingsToCSS(settings) → string
export function useChatSettings()
```

### src/hooks/useElectronPreview.ts
```
export function useElectronPreview()
```

### src/hooks/useYouTubeChat.ts
```
export const useYouTubeChat = (apiKey, videoId) =>
```

### src/index.css
```
.custom-scrollbar
.custom-scrollbar
.custom-scrollbar
.custom-scrollbar
.glass-panel
.inner-glow-stroke
.chat-preview-container
.chat-preview-container
```

### src/services/ChatPollingService.ts
```
export interface PollingOptions
export interface PollingResult
export class ChatPollingService
  constructor(youtubeService, options)
  async startPolling(videoId) → Promise<boolean>
  stopPolling() → void
  isActive() → boolean
  getMessages() → YouTubeChatMessage[]
  getMessageCount() → number
  clearMessages() → void
```

### src/services/YouTubeService.ts
```
export interface ChatFetchResult
export enum ConnectionStatus
export class YouTubeService
  constructor(apiKey)
  getApiKey() → string
  getConnectionStatus() → ConnectionStatus
  async validateApiKey() → Promise<boolean>
  async getLiveChatId(videoId) → Promise<string | nul
  async fetchChatMessages(liveChatId, pageToken?) → Promise<ChatFetchRes
  async isStreamLive(videoId) → Promise<boolean>
```

### src/types/app.ts
```
export interface ChatSettings
export interface Preset
export interface StreamInfo
export type Theme
export type MessageSpacing
export type AnimationSpeed
export type AnimationStyle
export type NameMessageLayout
export type BackgroundStyle
```

### src/types/youtube.ts
```
export interface YouTubeChatMessage
export interface YouTubeLiveStream
export interface YouTubeApiResponse
```

### src/utils/analytics.ts
```
export function getUserId() → string
export function setUserId(userId) → void
export function clearUserId() → void
export function isAnalyticsEnabled() → boolean
export function setAnalyticsEnabled(enabled) → void
export async function trackEvent(name, props?, unknown>, includeUserId) → Promise<void>
export function trackEventAsync(name, props?, unknown>, includeUserId) → void
```

### src/utils/cssExport.ts
```
export function generateOBSCSS(css, options) → string
export async function downloadCSSFile(cssContent, filename) → Promise<void>
```

### src/utils/cssGenerator.ts
```
export interface ContainerSettings
export interface MessageSettings
export interface UsernameSettings
export interface MessageTextSettings
export interface AvatarSettings
export interface TimestampSettings
export interface ScrollbarSettings
export interface GeneralSettings
export interface HeaderSettings
export interface ScrollButtonSettings
export interface EngagementMessagesSettings
export interface ChatDisclaimerSettings
export interface AnimationSettings
export interface LayoutSettings
export interface ChatCSSSettings
export type NameMessageLayout
export type BackgroundStyle
export function generateChatCSS(settings) → string
```

### src/utils/fonts.ts
```
export interface FontOption
export function getFontUrl(fontFamily) → string
export function loadWebFont(fontFamily) → void
```

### src/utils/formatters.ts
```
export const formatTimestamp = (date) =>
export const sanitizeMessage = (message) =>
export const truncateText = (text, maxLength) =>
export const formatViewerCount = (count) =>
```

### src/utils/storage.ts
```
export function getStoredApiKey() → string | null
export function storeApiKey(key) → void
export function clearStoredApiKey() → void
export function isValidApiKeyFormat(key) → boolean
export function hasStoredApiKey() → boolean
```

### src/utils/youtubeMetadata.ts
```
export interface YouTubeVideoInfo
export type YouTubeFetchResult
export async function fetchYouTubeMetadata(videoId) → Promise<YouTubeFetchResult>
```

### src/utils/youtubeValidation.ts
```
export interface YouTubeUrlValidation
export function validateYouTubeUrl(input) → YouTubeUrlValidation
```

## src-tauri

### src-tauri/Cargo.toml
```
table [package]
table [lib]
table [build-dependencies]
table [dependencies]
key name
key version
key description
key authors
key license
key repository
key edition
key rust-version
key serde_json
key log
key tauri-plugin-log
key tauri-plugin-aptabase
key tauri-plugin-dialog
key tauri-plugin-fs
key dotenvy
key sentry
key sentry-rust-minidump
key sentry-log
key time
```

### src-tauri/src/lib.rs
```
pub fn run()
```

### src-tauri/icons/android/mipmap-anydpi-v26/ic_launcher.xml
```
root adaptive-icon
```

### src-tauri/icons/android/values/ic_launcher_background.xml
```
root resources
color[name=ic_launcher_background]
```

### src-tauri/src/sentry.rs
```
pub fn add_breadcrumb(category: &str, message: &str, level: SentryLevel)
pub fn capture_error(err: &str)
pub fn capture_message(message: &str, level: SentryLevel)
pub fn send_test_log()
pub fn trigger_test_panic()
pub fn send_fake_crash_event()
pub fn send_fake_error_with_stacktrace()
pub fn send_test_scenario()
pub fn init_sentry() → sentry::ClientInitGuard
```

## stitch

### stitch/asset_manager_list_preview_view/code.html
```
title: StreamStyle Asset Manager
```

### stitch/pro_grade_creator_suite/DESIGN.md
```
h2 Brand & Style
h2 Colors
h2 Typography
h2 Layout & Spacing
h2 Elevation & Depth
h2 Shapes
h2 Components
```

### stitch/refined_editor_workspace/code.html
```
title: StreamStyle | YouTube Live Chat CSS Editor
```

### stitch/settings_consistent_nav/code.html
```
title: OBS Integration Guide - StreamStyle
button#copyBtn
div#toast
```
